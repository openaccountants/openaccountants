# CLA signature store

This branch exists only so `contributor-assistant/github-action` can record CLA
signatures at `signatures/cla.json`. It is not part of the project source.

Keep this branch UNPROTECTED: the action commits to it on every signature, and
branch protection makes that write fail.
